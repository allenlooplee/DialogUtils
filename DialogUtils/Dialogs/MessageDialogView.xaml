<UserControl
    x:Class="DialogUtils.Dialogs.MessageDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Width="240" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="{StaticResource SmallBottomMargin}"
            FontSize="{StaticResource LargeFontSize}"
            Text="{Binding Header}"
            Visibility="{Binding Header, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter='inverse'}" />
        <TextBlock
            Grid.Row="1"
            Text="{Binding Message}"
            TextWrapping="Wrap" />
        <DockPanel
            Grid.Row="2"
            Margin="{StaticResource MediumTopMargin}"
            LastChildFill="False">
            <Button
                Command="{x:Static md:DialogHost.CloseDialogCommand}"
                Content="取消"
                DockPanel.Dock="Right"
                Style="{StaticResource MaterialDesignFlatButton}"
                Visibility="{Binding IsCancelButtonVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.CommandParameter>
                    <sys:Boolean>false</sys:Boolean>
                </Button.CommandParameter>
            </Button>
            <Button
                Command="{x:Static md:DialogHost.CloseDialogCommand}"
                Content="确定"
                DockPanel.Dock="Right"
                Style="{StaticResource MaterialDesignFlatButton}">
                <Button.CommandParameter>
                    <sys:Boolean>true</sys:Boolean>
                </Button.CommandParameter>
            </Button>
        </DockPanel>
    </Grid>
</UserControl>
